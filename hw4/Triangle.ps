Texture2D colorTexture : register(t0);
SamplerState colorSampler : register(s0);

struct VSOutput
{
    float4 pos : SV_Position;
    float2 uv : TEXCOORD;
};

float4 main(VSOutput pixel) : SV_Target
{
    return float4(colorTexture.Sample(colorSampler, pixel.uv).xyz, 1.0f);
}
